# Reproduce a D-State Task by Freezing a Filesystem

> Work in progress.

This lab reproducer will demonstrate how a task can enter uninterruptible sleep (`D` state) when it attempts to access a frozen filesystem.

## Status

README content is still being drafted.
